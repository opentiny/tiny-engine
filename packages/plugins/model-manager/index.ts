import entry from './src/Main.vue'
import metaData from './meta'

export default {
  ...metaData,
  entry
  // apis: {},      // 预留，后续可暴露API
  // metas: [],     // 预留，后续可注册元服务
  // options: {},   // 预留，后续可加自定义配置
  // components: {} // 预留，后续可加子组件
} 